# Authentication Architecture

This directory contains the structural frameworks, process workflows, and logical diagrams representing the enterprise MFA authentication topology.

- **Primary Identity Provider (IdP):** Enforces TOTP/FIDO2 challenges prior to directory access.
- **Access Policies:** Conditional access rules based on user role and IP trust level.
